# NOTE:
# Previous config used /tmp for pnpm store/virtual-store. Some environments purge /tmp,
# which breaks node_modules symlinks (e.g. vitest.mjs missing) and makes CI/tests flaky.
# Keep defaults for stability.
