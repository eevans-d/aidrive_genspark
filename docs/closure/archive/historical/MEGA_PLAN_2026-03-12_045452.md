# Mega Plan - Verified Addendum Parallel B

**Fecha:** 2026-03-12 04:54 UTC
**Fuente auditada:** `docs/audit/EXHAUSTIVE_SCAN_REPORT_2026-03-12_PARALLEL_B.md`
**Objetivo:** revalidar el reporte Parallel B contra la fuente canonica del repo y convertirlo en un plan ejecutable con gates verificables.

## Veredicto ajustado

- Estado general verificado: `operable` y alineado con `LISTO PARA PRODUCCION`, pero con gaps operativos reales en observabilidad/smoke y ruido de calidad no bloqueante.
- Hallazgos del reporte que siguen abiertos o pendientes de evidencia runtime: `EXH-B-001`, `EXH-B-002`, `EXH-B-003`, `EXH-B-009`, `EXH-B-011`.
- Hallazgos del reporte que quedan cerrados por evidencia fresca del mismo `2026-03-12`: `EXH-B-004`, `EXH-B-005`, `EXH-B-006`, `EXH-B-007`, `EXH-B-008`, `EXH-B-010`.
- Reencuadre clave:
  - `EXH-B-001` no debe resolverse abriendo `api-proveedor` al publico por defecto. La politica vigente (`D-086`) mantiene `verify_jwt=true` para todo excepto `api-minimarket`; por lo tanto la opcion recomendada es **token tecnico dedicado + `x-api-secret`**, no `verify_jwt=false`.
  - `EXH-B-006` y `EXH-B-007` quedan cerrados en repo tras el hardening del harness frontend y la limpieza completa de lint.

## Verificaciones ejecutadas

- `pnpm -C minimarket-system build` -> `PASS` sin warnings de chunking/PWA.
- `pnpm -C minimarket-system lint` -> `0 errors, 0 warnings`.
- `pnpm -C minimarket-system test:components` -> `257/257 PASS` en corridas consecutivas.
- `npm run test:unit` -> `1959/1959 PASS`.
- `npm run docs:context-budget` -> `ok=9 warn=0 fail=0`.
- `node scripts/validate-doc-links.mjs` -> `PASS`.
- `node scripts/check-critical-deps-alignment.mjs` -> `PASS`.
- `node scripts/check-supabase-js-alignment.mjs` -> `PASS`.
- `node scripts/metrics.mjs --check` -> `docs/METRICS.md OK`.
- `bash scripts/run-integration-tests.sh --dry-run` -> prerequisitos OK.
- `bash scripts/run-e2e-tests.sh --dry-run` -> prerequisitos OK.
- `npm audit --omit=dev --audit-level=high` -> `0 vulnerabilities`.
- `pnpm -C minimarket-system audit --prod --audit-level high` -> `No known vulnerabilities found`.
- `python3 .agent/scripts/env_audit.py --with-supabase` -> faltantes en secrets detectados (names only).
- `npm run ops:env-contract` -> `PASS`; sin faltantes `required` para `prod`.
- `supabase --version` -> `2.72.7`.

## Limites de verificacion

- Smoke remoto no fue reproducible en esta sesion porque `SUPABASE_URL` y credenciales operativas no estaban cargadas.
- Smoke local no fue reproducible sin intervenir otro stack Docker: `supabase start` fallo por colision en puerto `54322`.
- Aun con esa limitacion, el contrato de auth de `api-proveedor/health` queda respaldado por:
  - politica `D-086`
  - `tests/e2e/api-proveedor.smoke.test.ts`
  - `scripts/ops-smoke-check.mjs`

## Production Readiness Score

**Score verificado para este eje operativo:** `84/100`

| Gate | Peso | Resultado |
|---|---:|---:|
| Tests pasan | 15 | 15 |
| Build OK | 10 | 10 |
| Security limpio | 15 | 11 |
| UX/Test signal | 10 | 7 |
| Cron jobs / smoke operativo | 10 | 4 |
| Docs actualizados | 10 | 10 |
| Deploy script seguro | 10 | 10 |
| Performance baseline | 10 | 9 |
| Env vars alineadas | 5 | 3 |
| Rollback / runbook | 5 | 5 |

## Prioridades (Top 8)

1. **OPS-001 - Formalizar auth tecnica para `ops:smoke` de `api-proveedor`**
   - DoD: `ops:smoke` documenta y acepta `OPS_SMOKE_API_PROVEEDOR_AUTHORIZATION` + `OPS_SMOKE_API_PROVEEDOR_SECRET`, y el check remoto queda ejecutable en CI/nightly sin cambiar `verify_jwt=true`.
   - Rutas: `scripts/ops-smoke-check.mjs`, `tests/e2e/api-proveedor.smoke.test.ts`, `docs/API_README.md`, `docs/DECISION_LOG.md`.
   - Verificacion: `OPS_SMOKE_TARGET=remote ... node scripts/ops-smoke-check.mjs`.
   - Rollback: mantener smoke en modo warning-only si la credencial tecnica aun no esta disponible.

2. **OPS-002 - Integrar `ops:smoke` en nightly**
   - DoD: `.github/workflows/nightly-gates.yml` agrega job dedicado con resumen y artifact.
   - Rutas: `.github/workflows/nightly-gates.yml`, `package.json`.
   - Verificacion: workflow `nightly-gates` ejecuta `npm run ops:smoke`.
   - Rollback: fase 1 no bloqueante; promover a blocking solo tras 5 corridas verdes consecutivas.

3. **OPS-003 - Cubrir `cron-health-monitor/health-check` en CI**
   - DoD: secret tecnico disponible en CI para habilitar el check opcional del cron monitor.
   - Rutas: `scripts/ops-smoke-check.mjs`, `.github/workflows/nightly-gates.yml`, runbook operativo.
   - Verificacion: resumen de smoke incluye `cron-health-monitor/health-check`.
   - Rollback: mantenerlo como check no critico si falta la credencial.

4. **TEST-001 - Bajar ruido del harness frontend**
   - DoD: warnings `act(...)` reducidos en `Dashboard`/`Clientes` y flags Router v7 encapsuladas o silenciadas de forma controlada.
   - Rutas: `minimarket-system/src/pages/a11y.test.tsx`, test utils compartidos, paginas `Dashboard` y `Clientes`.
   - Verificacion: `pnpm -C minimarket-system test:components` verde y estable en dos corridas consecutivas.
   - Estado posterior: **cerrado en repo**.

5. **ENV-001 - Matriz de secretos requerida/opcional por entorno**
   - DoD: documento o fuente canonica con clasificacion `required/optional` por `dev/staging/prod` y chequeo predeploy.
   - Rutas: `.env.example`, `.agent/scripts/env_audit.py`, `docs/API_README.md` o doc nueva de entorno.
   - Verificacion: `python3 .agent/scripts/env_audit.py --with-supabase` sin falsos positivos criticos.
   - Estado posterior: **cerrado en repo** con `docs/ENV_SECRET_CONTRACT.*` + `npm run ops:env-contract`.

6. **LINT-001 - Reducir `72` warnings de `no-explicit-any`**
   - DoD: bajar el total al menos a `<=40` concentrando hotspots de mocks y tests.
   - Rutas: `minimarket-system/src/mocks/supabaseMock.ts`, `minimarket-system/src/pages/*.test.tsx`, `minimarket-system/src/hooks/__tests__/*`.
   - Verificacion: `pnpm -C minimarket-system lint`.
   - Estado posterior: **cerrado en repo** (`0 errors, 0 warnings`).

7. **LOG-001 - Estandarizar logs de scripts operativos**
   - DoD: scripts de smoke/seed/perf usan formato consistente (`[INFO]`, `[WARN]`, `[ERROR]`) con payload minimo.
   - Rutas: `scripts/*.mjs`.
   - Verificacion: `rg -n "console\\.(warn|error)" scripts`.
   - Estado posterior: **parcialmente mitigado**; los gate scripts principales usan `scripts/_shared/cli-log.mjs`.

8. **TOOL-001 - Normalizar version de Supabase CLI**
   - DoD: version objetivo documentada y alineada con CI.
   - Rutas: `AGENTS.md`, docs de onboarding, workflow/setup si aplica.
   - Verificacion: `supabase --version`.
   - Estado posterior: **parcial**; target `2.75.0` documentado, instalacion local aun en `2.72.7`.

## Secuencia recomendada

### Bloque A - 0 a 24h

- Ejecutar `OPS-001` y `OPS-002` juntos.
- Mantener como decision explicita: `api-proveedor` **no** cambia a `verify_jwt=false` sin nueva decision log.
- Evidencia requerida:
  - log de `ops:smoke`
  - diff del workflow nightly
  - nota operacional con nombres de secrets requeridos

### Bloque B - 2 a 5 dias

- Ejecutar `OPS-003`, `TEST-001`, `ENV-001`.
- Objetivo: eliminar puntos ciegos de monitoreo y mejorar la senal de CI.
- Evidencia requerida:
  - corrida nightly con smoke
  - `test:components` repetible
  - env audit con clasificacion de faltantes
- Estado posterior:
  - `TEST-001` cerrado
  - `ENV-001` cerrado
  - `OPS-003` implementado en repo y pendiente de evidencia runtime

### Bloque C - 1 a 2 semanas

- Ejecutar `LINT-001`, `LOG-001`, `TOOL-001`.
- Objetivo: reducir deuda operativa sostenida sin abrir cambios de riesgo alto.
- Evidencia requerida:
  - delta de warnings lint
  - sample de logs normalizados
  - version target de CLI documentada
- Estado posterior:
  - `LINT-001` cerrado
  - `LOG-001` parcial
  - `TOOL-001` parcial

## Gates de cierre

- `npm run test:unit`
- `pnpm -C minimarket-system test:components`
- `pnpm -C minimarket-system lint`
- `pnpm -C minimarket-system build`
- `node scripts/check-critical-deps-alignment.mjs`
- `node scripts/check-supabase-js-alignment.mjs`
- `npm run docs:context-budget`
- `node scripts/validate-doc-links.mjs`
- `node scripts/metrics.mjs --check`
- `python3 .agent/scripts/env_audit.py --with-supabase`

## Criterio de cierre del addendum

- `EXH-B-004`, `EXH-B-005`, `EXH-B-010` se consideran cerrados y no deben volver a priorizarse salvo regresion verificable.
- `EXH-B-006`, `EXH-B-007`, `EXH-B-008` quedan cerrados en repo por evidencia posterior a este addendum.
- `EXH-B-001` queda cerrado solo cuando exista auth tecnica estable para smoke sin romper `D-086`.
- `EXH-B-002` queda cerrado solo cuando `nightly-gates` ejecute `ops:smoke` con evidencia archivada.
