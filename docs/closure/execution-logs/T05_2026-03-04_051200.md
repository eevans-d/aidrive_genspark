# T05 — Idempotencia en POST /deposito/ingreso (ID-02)
- Estado: DONE
- Fecha: 2026-03-04 05:12 UTC
- Responsable: Claude Code
- Archivos tocados:
  - `supabase/functions/api-minimarket/index.ts` (handler `/deposito/ingreso`: skip precios_compra on replay, 200 vs 201)
- Comandos ejecutados:
  - Revision de `sp_movimiento_inventario` en migracion `20260302010000`
  - Revision de handler en index.ts
- Resultado:
  - La idempotencia ya estaba parcialmente implementada:
    - Header `idempotency-key` se lee y pasa a `sp_movimiento_inventario`
    - SP retorna `idempotent: true` si la key ya fue usada, sin duplicar movimiento
  - Fix aplicado (gap ID-02):
    - Si `movimiento.idempotent === true`, se SALTA la insercion a `precios_compra` (evita duplicados en replay)
    - Se retorna 200 (no 201) con mensaje indicando operacion idempotente
  - Resultado: reintento con misma key → mismo resultado, sin duplicado de inventario ni de precio
- Evidencia:
  - `supabase/functions/api-minimarket/index.ts:1693-1717`
  - `supabase/migrations/20260302010000_add_idempotency_movimientos_deposito.sql` (SP con check)
- Riesgos/rollback:
  - Bajo. Si se necesita re-insertar precio_compra en replay, quitar la condicion `!isIdempotentReplay`
- Siguiente accion:
  - T06 (Idempotencia en POST /compras/recepcion)
