# T02 — Lock de concurrencia OCR en gateway (RC-01)
- Estado: DONE
- Fecha: 2026-03-04 05:04 UTC
- Responsable: Claude Code
- Archivos tocados:
  - `supabase/functions/api-minimarket/helpers/supabase.ts` (nueva funcion `updateTableConditional`)
  - `supabase/functions/api-minimarket/index.ts` (import + handler `/facturas/:id/extraer` con lock atomico)
  - `supabase/functions/facturas-ocr/helpers.ts` (agregar `extrayendo` a VALID_FACTURA_OCR_EXTRAER_ESTADOS)
  - `supabase/functions/facturas-ocr/index.ts` (soporte `estado_previo` para cleanup en reintento)
  - `tests/unit/ocr-concurrency-lock.test.ts` (8 tests nuevos)
- Comandos ejecutados:
  - `npx vitest run tests/unit/ocr-concurrency-lock.test.ts` → 8/8 PASS
- Resultado:
  - Gateway ahora hace PATCH atomico `facturas_ingesta` con filtro `id=eq.X&estado=in.(pendiente,error)` para transicionar a `extrayendo`
  - Si PATCH retorna 0 filas → 409 OCR_IN_PROGRESS (otra extraccion ya esta en curso)
  - Rollback de estado en caso de fallo de invocacion OCR (network error, timeout)
  - Cleanup de estado a `error` si facturas-ocr falla y el estado quedo en `extrayendo`
  - facturas-ocr acepta `extrayendo` como estado valido y usa `estado_previo` del body para decidir cleanup
- Evidencia:
  - `tests/unit/ocr-concurrency-lock.test.ts` (8 PASS)
- Riesgos/rollback:
  - Si el nuevo estado `extrayendo` causa problemas con consultas existentes, revertir al flujo sin lock
  - Rollback: quitar `extrayendo` de helpers.ts y restaurar handler original
- Siguiente accion:
  - T03 (Transaccionalidad de insercion OCR)
