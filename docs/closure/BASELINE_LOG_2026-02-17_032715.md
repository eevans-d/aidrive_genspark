> [SNAPSHOT PRE-DEPLOY: 2026-02-17] Este documento refleja el estado **antes** del deploy de hardening D-126/D-127.
> Para estado post-deploy vigente, consultar `docs/ESTADO_ACTUAL.md` y `docs/closure/EVIDENCIA_DEPLOY_HARDENING_2026-02-17.md`.

# Baseline Log

- Date: `2026-02-17T03:27:15+00:00`
- Repo: `/home/eevan/ProyectosIA/aidrive_genspark`
- Supabase project_ref: `dqaygmjpzoqjjrywdsxi`

## date

```bash
date 
```

```text
Tue Feb 17 03:27:15 UTC 2026
```

## git status

```bash
git status --porcelain=v1 
```

```text
?? docs/closure/BASELINE_LOG_2026-02-17_032715.md
```

## git branch

```bash
git rev-parse --abbrev-ref HEAD 
```

```text
main
```

## git head

```bash
git rev-parse HEAD 
```

```text
b78426f12b996d305b8bae6a02f8c12664c2d555
```

## git log

```bash
git log -n 10 --oneline --decorate 
```

```text
b78426f (HEAD -> main) docs: D-121 channel matrix + D-122 stale path annotations across 14 docs
dadb210 (origin/main, origin/HEAD) docs: coherence audit — fix stale counts, paths, and ambiguous scores
66b2e56 fix(dx): Proveedores/Pedidos test harness + lint-staged eslint resolution
6a926a6 feat(tests): coverage global ≥80% — 11 test files nuevos + exclusiones + doc sync
298124e chore(closure): P2 fixes applied to remote + evidence
aa03471 fix(security): close P2 precios_proveedor RLS gap + scraper CORS wildcard
d9760e9 docs(closure): resume after interruption and align pending P2 continuity
7ac4848 docs(closure): register P2 pre-existing findings in OPEN_ISSUES
17b00f7 merge: P0 remediation + origin sync + integration gates (PR #71)
0d1fb03 docs(docuguard): add sync report and clarify forensic historical section
```

## supabase version

```bash
supabase --version 
```

```text
2.72.7
A new version of Supabase CLI is available: v2.75.0 (currently installed v2.72.7)
We recommend updating regularly for new features and bug fixes: https://supabase.com/docs/guides/cli/getting-started#updating-the-supabase-cli
```

## supabase migration list (linked)

```bash
supabase migration list --linked 
```

```text
Cannot find project ref. Have you run supabase link?
Try rerunning the command with --debug to troubleshoot the error.
```

## supabase functions list (safe view)

```bash
supabase functions list --project-ref "dqaygmjpzoqjjrywdsxi" --output json | jq -r '.[] | "\(.name)\tv\(.version)\tverify_jwt=\(.verify_jwt)"' | sort
```

```text
alertas-stock	v16	verify_jwt=true
alertas-vencimientos	v16	verify_jwt=true
api-minimarket	v26	verify_jwt=false
api-proveedor	v18	verify_jwt=true
cron-dashboard	v16	verify_jwt=true
cron-health-monitor	v16	verify_jwt=true
cron-jobs-maxiconsumo	v18	verify_jwt=true
cron-notifications	v24	verify_jwt=true
cron-testing-suite	v17	verify_jwt=true
notificaciones-tareas	v18	verify_jwt=true
reportes-automaticos	v16	verify_jwt=true
reposicion-sugerida	v16	verify_jwt=true
scraper-maxiconsumo	v19	verify_jwt=true
```

## supabase secrets list (names only)

```bash
supabase secrets list --project-ref "dqaygmjpzoqjjrywdsxi" --output json | jq -r '.[].name' | sort
```

```text
ALLOWED_ORIGINS
API_PROVEEDOR_SECRET
NOTIFICATIONS_MODE
SENDGRID_API_KEY
SMTP_FROM
SMTP_HOST
SMTP_PASS
SMTP_PORT
SMTP_USER
SUPABASE_ANON_KEY
SUPABASE_DB_URL
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_URL
```

